---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - vpa
  - influxdb
  - grafana-operator
  # grafana and grafana-mcp require the operator CRDs to be installed first
  # They will be added after operator is running
