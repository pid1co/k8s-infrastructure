---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: influxdb
  namespace: observability
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`influxdb.ts.pid1.co`)
      kind: Rule
      services:
        - name: influxdb-influxdb2
          port: 80
  tls: {}
