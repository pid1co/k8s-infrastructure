---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openclaw-trading-secrets
  namespace: openclaw-trading
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: openclaw-trading-secrets
    creationPolicy: Owner
  data:
    # FMP API key - real token, does not go through ClawShell
    - secretKey: fmp-api-key
      remoteRef:
        key: OpenClaw Trading/fmp_api_key
    # Telegram bot token - real token, does not go through ClawShell
    - secretKey: telegram-bot-token
      remoteRef:
        key: OpenClaw Trading/telegram_bot_token
