---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - crd.yaml
# Note: The OpenClaw operator deployment would be added here
# when the operator is available. For now, we deploy instances
# directly as Deployments using the CRD for documentation.
