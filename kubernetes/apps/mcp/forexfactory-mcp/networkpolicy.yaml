---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: forexfactory-mcp-policy
  namespace: mcp-servers
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: forexfactory-mcp
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow kubelet health checks (from node network)
    - from:
        - ipBlock:
            cidr: 192.168.0.0/16
      ports:
        - protocol: TCP
          port: 8080
    # Allow from openclaw-trading namespace
    - from:
        - namespaceSelector:
            matchLabels:
              openclaw.io/instance: trading
      ports:
        - protocol: TCP
          port: 8080
  egress:
    # DNS
    - to:
        - namespaceSelector: {}
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # ForexFactory website
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 80
