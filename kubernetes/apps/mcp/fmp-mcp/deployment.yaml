---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fmp-mcp
  namespace: mcp-servers
  labels:
    app.kubernetes.io/name: fmp-mcp
    app.kubernetes.io/component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fmp-mcp
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fmp-mcp
        app.kubernetes.io/component: mcp-server
    spec:
      containers:
        - name: fmp-mcp
          image: ghcr.io/your-org/fmp-mcp-server:latest
          env:
            - name: FMP_API_KEY
              valueFrom:
                secretKeyRef:
                  name: fmp-mcp-secrets
                  key: fmp-api-key
            - name: MCP_SERVER_PORT
              value: "8080"
          ports:
            - name: http
              containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: fmp-mcp
  namespace: mcp-servers
spec:
  selector:
    app.kubernetes.io/name: fmp-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
