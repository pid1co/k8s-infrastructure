---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: onepassword-connect
  namespace: external-secrets
spec:
  replicas: 1
  selector:
    matchLabels:
      app: onepassword-connect
  template:
    metadata:
      labels:
        app: onepassword-connect
    spec:
      securityContext:
        runAsUser: 999
        runAsGroup: 999
        fsGroup: 999
      initContainers:
        - name: init-credentials
          image: busybox
          command:
            - /bin/sh
            - -c
            - |
              cp /secret/1password-credentials.json /home/opuser/.op/
              chmod 600 /home/opuser/.op/1password-credentials.json
          volumeMounts:
            - name: credentials
              mountPath: /secret
              readOnly: true
            - name: op-data
              mountPath: /home/opuser/.op
      containers:
        - name: connect-api
          image: 1password/connect-api:latest
          ports:
            - containerPort: 8080
          env:
            - name: OP_SESSION
              value: /home/opuser/.op/1password-credentials.json
          volumeMounts:
            - name: op-data
              mountPath: /home/opuser/.op
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
        - name: connect-sync
          image: 1password/connect-sync:latest
          ports:
            - containerPort: 8081
          env:
            - name: OP_SESSION
              value: /home/opuser/.op/1password-credentials.json
          volumeMounts:
            - name: op-data
              mountPath: /home/opuser/.op
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
      volumes:
        - name: credentials
          secret:
            secretName: onepassword-connect-credentials
        - name: op-data
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: onepassword-connect
  namespace: external-secrets
spec:
  selector:
    app: onepassword-connect
  ports:
    - name: api
      port: 8080
      targetPort: 8080
    - name: sync
      port: 8081
      targetPort: 8081
